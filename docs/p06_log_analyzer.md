# Практикум 6: `std::string` — Створюємо "Аналізатор логів"

## 🎯 Наш "продукт" сьогодні

Уявимо, що ми — системні адміністратори, і у нас є "брудний" лог-файл. Наша задача — написати утиліту, яка "парсить" (розбирає) цей лог і виводить дані у красивому, читабельному форматі. Це одна з найпоширеніших задач у програмуванні.

**Наш "брудний" лог:**

```
[INFO] user: admin session_id: 1001 action: login
[ERROR] user: guest session_id: 1002 action: access_denied
[INFO] user: dev session_id: 1003 action: logout
```

**Наш "чистий" звіт (мета):**

```
РІВЕНЬ | КОРИСТУВАЧ | ID СЕСІЇ | ДІЯ
-----------------------------------------------------
INFO   | admin      | 1001     | login
ERROR  | guest      | 1002     | access_denied
INFO   | dev        | 1003     | logout
```

Для цього нам знадобиться весь арсенал `std::string`.

## 🏁 Експрес-опитування

У лекції 9 ми вже познайомилися з `std::string`. Давайте пригадаємо:

1.  У чому головна перевага `std::string` над `char[]` (C-String)?
2.  Як з'єднати (конкатенувати) два `std::string`?
3.  Чому `cin >> myString;` — погана ідея для зчитування повного імені?
4.  Який заголовочний файл потрібно підключити для роботи з `std::string`?

<details markdown="1">
<summary>Натисніть, щоб побачити відповіді</summary>

1.  **Безпека та зручність.** `std::string` **автоматично керує пам'яттю**, тому ми не можемо отримати "переповнення буфера". Він також надає зручні методи (`.length()`, `.find()` тощо).
2.  За допомогою оператора **`+`** (наприклад, `string s3 = s1 + s2;`).
3.  Тому що `cin >>` зчитує лише **одне слово** (до першого пробілу). Для зчитування повного рядка потрібен `getline`.
4.  Потрібно підключити **`<string>`**.

</details>

-----

## Частина 1: Зчитування логу (Згадуємо `getline` та `stringstream`)

Ми не будемо читати реальний файл (це тема Лекції 11), а використаємо `stringstream`, щоб симулювати потік даних з файлу.

**Ключові інструменти:**

  * `#include <sstream>`: Дозволяє "обгорнути" рядок у потік.
  * `std::getline(stream, string)`: Читає дані з потоку `stream` у `string` **по одному рядку** (до символу `\n`).

<!-- end list -->

```cpp
#include <iostream>
#include <string>
#include <sstream> // Для "симуляції" файлу
#include <iomanip> // Для красивого виводу
#include <algorithm> // Для replace (поки не використовуємо, але добре знати)

using namespace std;

// Наш "брудний" лог-файл у вигляді одного великого рядка
string rawLog = 
    "[INFO] user: admin session_id: 1001 action: login\n"
    "[ERROR] user: guest session_id: 1002 action: access_denied\n"
    "[INFO] user: dev session_id: 1003 action: logout\n";

int main() {
    // 1. "Заряджаємо" наш рядок у stringstream
    stringstream ss(rawLog);
    string line; // Змінна для зберігання одного рядка

    // 2. Зчитуємо "файл" по одному рядку, поки він не закінчиться
    while (getline(ss, line)) {
        // Тут ми будемо "парсити" кожен рядок 'line'
        cout << "Щойно зчитано: " << line << endl;
    }
    return 0;
}
```

-----

## Частина 2: "Парсинг" — ріжемо рядок скальпелем (`find` та `substr`)

Тепер найцікавіше. Нам потрібно "вирізати" корисні дані з `line`.

**Ключові інструменти:**

  * **`str.find(text)`**: Шукає `text` всередині `str`. Повертає індекс (позицію) **початку** знайденого тексту. Якщо нічого не знайдено, повертає `string::npos`.
  * **`str.substr(start_index, length)`**: "Вирізає" шматок рядка, починаючи з `start_index` довжиною `length`.

Давайте напишемо функцію-парсер:

```cpp
// Функція для "вирізання" значення між двома тегами
string parseValue(const string& line, string startTag) {
    // 1. Знаходимо позицію, де починається наш тег (напр., "user: ")
    size_t startPos = line.find(startTag);
    if (startPos == string::npos) { // Якщо тег не знайдено
        return "N/A";
    }
    
    // 2. Обчислюємо, де починається саме ЗНАЧЕННЯ
    // (позиція тегу + його довжина)
    startPos += startTag.length();

    // 3. Знаходимо, де закінчується наше значення
    // (шукаємо наступний пробіл, *починаючи* з startPos)
    size_t endPos = line.find(" ", startPos);

    // 4. "Вирізаємо" підрядок
    // substr(початок, довжина)
    return line.substr(startPos, endPos - startPos);
}

// ... всередині main() ...
while (getline(ss, line)) {
    string level = parseValue(line, "[");
    level.pop_back(); // Прибираємо ']' в кінці (приклад прямої маніпуляції)

    string user = parseValue(line, "user: ");
    string session = parseValue(line, "session_id: ");
    
    // Для "action" нам потрібен останній шматок, 
    // тому substr можна викликати з одним параметром
    size_t actionPos = line.find("action: ") + 8;
    string action = line.substr(actionPos);

    cout << "Рівень: " << level 
         << ", Користувач: " << user 
         << ", Сесія: " << session 
         << ", Дія: " << action << endl;
}
```

### ❓ Питання до групи

`size_t startPos = line.find(startTag);` — `size_t`? Що це за тип? Чому не `int`?

<details markdown="1">
<summary>Натисніть, щоб побачити відповідь</summary>
`size_t` — це спеціальний беззнаковий цілочисельний тип, який гарантовано може вмістити розмір *будь-якого* об'єкта в пам'яті (або індекс масиву/рядка). Він вважається більш "правильним" для індексів та розмірів, ніж `int`, оскільки розмір не може бути від'ємним, а `int` може "переповнитись", якщо об'єкт *дуже* великий (хоча для `string` це рідкість).
</details>

-----

## Частина 3: Форматований вивід (`<iomanip>`)

Тепер, коли у нас є дані, давайте виведемо їх красиво.

**Ключові інструменти:**

  * `#include <iomanip>`
  * `setw(n)`: Встановлює ширину *наступного* поля.
  * `left` / `right`: Вирівнювання (притиснути до лівого/правого краю).
  * `setfill(char)`: Чим заповнити порожнє місце.

<!-- end list -->

```cpp
// ... всередині main(), ДО циклу while ...
// Друкуємо заголовок
cout << left; // Встановлюємо вирівнювання по лівому краю
cout << setw(7) << "РІВЕНЬ" << "| " 
     << setw(10) << "КОРИСТУВАЧ" << "| " 
     << setw(10) << "ID СЕСІЇ" << "| " 
     << "ДІЯ" << endl;
cout << setfill('-') << setw(50) << "" << setfill(' ') << endl;

// ... всередині циклу while, ЗАМІСТЬ старого cout ...
cout << left; // Вирівнювання
cout << setw(7) << level << "| " 
     << setw(10) << user << "| " 
     << setw(10) << session << "| " 
     << action << endl;
```

*Запустіть цей код, і ви отримаєте ідеальну таблицю, як ми і планували\!*

-----

## Частина 4: "Пастки" `std::string`

`std::string` — це дивовижний інструмент, але він має дві "пастки", успадковані з `TestStringAt.cpp` та `TestLength.cpp`.

### Пастка 1: `.at()` проти `[]` (Безпека vs Швидкість)

  * **`str[i]` (Оператор індексації):**

      * **Швидкий.**
      * **Небезпечний:** Не перевіряє, чи ви не вийшли за межі рядка. `str[9999]` (на короткому рядку) призведе до **невизначеної поведінки** і, ймовірно, до "падіння".

  * **`str.at(i)` (Метод):**

      * **Трохи повільніший** (бо виконує перевірку).
      * **Безпечний:** Перевіряє індекс. Якщо індекс за межами рядка, програма впаде з чіткою помилкою `std::out_of_range`.

> **Висновок:** Використовуйте `[]`, коли ви на 100% впевнені в логіці (напр., `for(int i=0; i < str.length()...)`). Використовуйте `.at()`, коли індекс приходить "ззовні" (напр., від користувача).

### Пастка 2: `str.length()` — це **байти**, а не **символи**\!

Подивіться на цей код з `TestLength.cpp`:

```cpp
string cyrillic_str = "Привіт";
string ascii_str = "Hello";

cout << cyrillic_str.length() << endl; // Виведе: 12
cout << ascii_str.length() << endl;  // Виведе: 5
```

**Чому?**
`std::string` зберігає текст у кодуванні UTF-8. Символи ASCII (A-Z) займають 1 байт. Символи кирилиці (`П`, `р`, `и`...) займають **2 байти кожен**.

`str.length()` (та `str.size()`) повертає кількість **БАЙТ** (`char`), а не "людських" символів.

**Що це означає на практиці:**
Якщо ви спробуєте змінити "перший" символ:

```cpp
cyrillic_str[0] = 'h'; 
// Ви зламали кодування! 
// "Привіт" (12 байт) -> [0xD0 0x9F] [0xD1 0x80] ...
// "hривіт" (11 байт) -> [0x68] [0xD1 0x80] ...
// Перший байт 0xD0 був замінений, а 0x9F залишився "сміттям".
cout << cyrillic_str << endl; // Виведе "hривіт" або інший "поламаний" рядок
```

> **Висновок:** `std::string` чудово *зберігає* UTF-8, але *маніпулювати* (змінювати, брати `substr`) рядками не-ASCII потрібно з великою обережністю, або за допомогою спеціальних бібліотек.

-----

## Контрольні питання

1.  **Парсинг.** Якби вам потрібно було знайти *друге* входження слова "user" у рядок, як би ви це зробили, використовуючи `find()`? (Підказка: `find()` має другий, необов'язковий параметр).
2.  **Безпека.** Чому `str.at(i)` вважається безпечнішим, ніж `str[i]`?
3.  **IO.** У чому фундаментальна різниця між `cin >> str;` та `getline(cin, str);`?
4.  **`iomanip`.** Яку команду з `<iomanip>` потрібно використати, щоб поле виводу `cout` мало ширину 15 символів і заповнювалося зірочками (`*`)?
5.  **"Пастка".** У вас є `string s = "Слава";`. Чому `s.length()` поверне `8`, а не `4`?

<details markdown="1">
<summary>Натисніть, щоб побачити відповіді</summary>

**1. Друге входження:**
Метод `find()` приймає другий параметр: `start_pos` (позиція, з якої починати пошук).

```cpp
string s = "user: admin, user: guest";
size_t firstPos = s.find("user"); // Знайде на позиції 0
// Починаємо шукати ЗНОВУ, але з позиції (firstPos + 1)
size_t secondPos = s.find("user", firstPos + 1); // Знайде на позиції 13
```

**2. Безпека `.at()` vs `[]`:**
`.at(i)` **перевіряє індекс** перед доступом. Якщо індекс виходить за межі рядка, `.at()` генерує чітку, зрозумілу помилку (exception `std::out_of_range`). `str[i]` **не перевіряє** індекс, що призводить до невизначеної поведінки (пошкодження пам'яті або "падіння" програми).

**3. `cin` vs `getline`:**

  * `cin >> str;` зчитує символи **до першого пробільного символу** (пробіл, tab, `\n`). Вона зчитує *одне слово*.
  * `getline(cin, str);` зчитує символи **до символу `\n`** (до натискання Enter). Вона зчитує *цілий рядок* з усіма пробілами.

**4. `iomanip`:**
Потрібно дві команди:

```cpp
cout << setfill('*') << setw(15) << "text";
```

**5. "Пастка" `length()`:**
`std::string::length()` повертає кількість **байт**, а не символів. Оскільки рядок у кодуванні UTF-8, кожен з 4-х символів кирилиці (`С`, `л`, `а`, `в`, `а`) займає 2 байти. Таким чином: 4 символи \* 2 байти/символ = 8 байт. (Насправді `а` - це 2 байти, `в` - 2, `л` - 2, `С` - 2, `а` - 2. "Слава" - 5 символів, 10 байт. "Сла" - 3*2=6. "Привіт" - 6*2=12). *Виправлення: "Слава" (5 символів) -> `[0xD0 0xA1][0xD0 0xBB][0xD0 0xB0][0xD0 B2][0xD0 0xB0]` = 10 байт. "Сла" = 6 байт.*

**Виправлена відповідь на 5:** `std::string::length()` повертає кількість **байт**, а не символів. Оскільки рядок у кодуванні UTF-8, кожен з 5-ти символів кирилиці (`С`, `л`, `а`, `в`, `а`) займає 2 байти. Таким чином: 5 символів \* 2 байти/символ = **10 байт**. `s.length()` поверне **10**.

</details>